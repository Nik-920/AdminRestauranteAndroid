<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    android:layout_marginHorizontal="0dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardUseCompatPadding="true"
    android:foreground="?android:attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="@color/white">

        <!-- Número de mesa -->
        <TextView
            android:id="@+id/tvNumeroMesa"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Mesa: 3"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Fecha del pedido -->

        <!-- Lista de platillos con cantidades -->

        <TextView
            android:id="@+id/tvFechaPedido"
            android:layout_width="270dp"
            android:layout_height="23dp"
            android:text="2025-07-04 17:34"
            android:textColor="@color/gris_oscuro"
            android:textSize="14sp"
            app:layout_constraintBaseline_toBaselineOf="@id/tvNumeroMesa"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.955"
            app:layout_constraintStart_toEndOf="@id/tvNumeroMesa" />

        <TextView
            android:id="@+id/tvDetallePlatillos"
            android:layout_width="381dp"
            android:layout_height="45dp"
            android:layout_marginTop="8dp"
            android:text="2×Ceviche, 1×Chicha Morada"
            android:textColor="@color/black"
            android:textSize="15sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvNumeroMesa" />

        <!-- Total de la cuenta -->
        <TextView
            android:id="@+id/tvCuentaTotal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Total: S/ 59.50"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/green"
            app:layout_constraintTop_toBottomOf="@id/tvDetallePlatillos"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Icono para ver/preparar -->
        <ImageView
            android:id="@+id/ivVerDetalles"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginTop="8dp"
            android:src="@drawable/icon_usuarios"
            android:contentDescription="Ver detalles"
            app:tint="@color/azul_atlantico"
            app:layout_constraintTop_toBottomOf="@id/tvDetallePlatillos"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/tvCuentaTotal"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
