<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema general de la app con ActionBar -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Habilitar el ActionBar -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>

        <!-- Respeto por la barra de estado y la toolbar -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:fitsSystemWindows">true</item>

        <!-- Estilo personalizado para el ActionBar -->
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
    </style>

    <!-- Estilo del ActionBar -->
    <style name="MyActionBarStyle" parent="Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- Color del texto del título -->
    <style name="MyActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/black</item>
    </style>
</resources>
